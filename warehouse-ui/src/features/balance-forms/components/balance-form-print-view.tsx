import type { BalanceForm } from '../types/balance-form.types'

// Placeholder — will be replaced in Task 9
export function buildPrintHtml(_form: BalanceForm): string {
  return '<html><body><p>Print view coming soon.</p></body></html>'
}
